import { faqs } from "@/lib/public-site/content";
export function FaqList({ limit }: { limit?: number }) { return <div className="mx-auto max-w-3xl divide-y divide-[#0b6b3a]/15 rounded-3xl border border-[#0b6b3a]/15 bg-white px-6">{faqs.slice(0, limit).map(([question, answer], index) => <details key={question} className="group py-5" open={index === 0}><summary className="flex cursor-pointer list-none items-center justify-between gap-5 font-semibold text-[#173d2a]">{question}<span className="text-xl text-[#c89b3c] group-open:rotate-45">+</span></summary><p className="pt-3 pr-10 text-sm leading-7 text-[#5b6c63]">{answer}</p></details>)}</div>; }
