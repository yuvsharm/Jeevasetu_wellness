export function SectionHeading({ eyebrow, title, copy, center = false }: { eyebrow: string; title: string; copy?: string; center?: boolean }) { return <div className={`mb-10 ${center ? "mx-auto max-w-2xl text-center" : "max-w-2xl"}`}><p className="eyebrow">{eyebrow}</p><h2 className="mt-3 font-serif text-4xl leading-tight text-[#103c27] sm:text-5xl">{title}</h2>{copy && <p className="mt-4 leading-7 text-[#5b6c63]">{copy}</p>}</div>; }
