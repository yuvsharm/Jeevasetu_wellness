"use client";
import {useMutation,useQuery,useQueryClient} from "@tanstack/react-query";
import {useState} from "react";
import {requestJson} from "@/lib/api/client";
import type {PractitionerApplication} from "@/lib/practitioners/contracts";
export function PractitionerReview(){const [status,setStatus]=useState("");const [search,setSearch]=useState("");const client=useQueryClient();const refresh=()=>client.invalidateQueries({queryKey:["practitioner-review"]});const query=useQuery({queryKey:["practitioner-review",status,search],queryFn:()=>requestJson<PractitionerApplication[]>(`/api/practitioners/applications?status=${status}&search=${encodeURIComponent(search)}`)});const review=useMutation({mutationFn:({id,action}:{id:string;action:string})=>requestJson(`/api/practitioners/applications/${id}/review`,{method:"POST",body:JSON.stringify({action,reason:action==="correction"?"Please review the highlighted professional evidence.":action==="reject"?"Submitted evidence could not be verified.":""})}),onSuccess:refresh});const verify=useMutation({mutationFn:({kind,id}:{kind:"documents"|"competencies";id:string})=>requestJson(`/api/practitioners/${kind}/${id}/verify`,{method:"POST",body:JSON.stringify({verified:true})}),onSuccess:refresh});return <section className="mt-8" aria-labelledby="practitioner-review-heading"><h2 id="practitioner-review-heading" className="text-2xl font-bold">Practitioner applications</h2><p className="mt-1 text-slate-600">Review structured qualifications, documents and competencies within your authorized clinic scope.</p><div className="mt-5 grid gap-3 sm:grid-cols-2"><input aria-label="Search applications" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search name, email or mobile" className="min-h-11 rounded-xl border px-3"/><select aria-label="Application status" value={status} onChange={e=>setStatus(e.target.value)} className="min-h-11 rounded-xl border px-3"><option value="">All statuses</option>{["SUBMITTED","UNDER_REVIEW","CORRECTION_REQUIRED","APPROVED","REJECTED"].map(item=><option key={item}>{item}</option>)}</select></div><div className="mt-5 grid gap-4 xl:grid-cols-2">{query.data?.map(item=><article key={item.id} className="rounded-2xl border bg-white p-5"><div className="flex flex-wrap justify-between gap-3"><div><h3 className="text-xl font-bold">{item.full_legal_name}</h3><p className="text-sm text-slate-500">{item.category.replaceAll("_"," ")} · {item.highest_qualification}</p></div><span className="rounded-full bg-slate-100 px-3 py-2 text-xs font-bold">{item.status.replaceAll("_"," ")}</span></div><dl className="mt-4 grid gap-3 text-sm sm:grid-cols-2"><div><dt className="font-bold">Experience</dt><dd>{item.experience_years}y {item.experience_months}m</dd></div><div><dt className="font-bold">Location</dt><dd>{item.city}, {item.state}</dd></div></dl><div className="mt-4 grid gap-3 text-sm sm:grid-cols-2"><div><h4 className="font-bold">Private documents</h4>{item.documents.map(doc=><div key={doc.id} className="mt-2 flex flex-wrap items-center gap-2"><a href={`/api/practitioners/documents/${doc.id}`} className="underline">{doc.original_name}</a><button onClick={()=>verify.mutate({kind:"documents",id:doc.id})} className="font-bold text-emerald-800">Verify</button></div>)}</div><div><h4 className="font-bold">Competencies</h4>{item.competencies.map(skill=><div key={skill.id} className="mt-2 flex flex-wrap items-center gap-2"><span>{skill.therapy_name}</span><button onClick={()=>verify.mutate({kind:"competencies",id:skill.id})} className="font-bold text-emerald-800">Verify</button></div>)}</div></div>{["SUBMITTED","UNDER_REVIEW"].includes(item.status)&&<div className="mt-5 flex flex-wrap gap-2"><button onClick={()=>review.mutate({id:item.id,action:"review"})} className="min-h-11 rounded-xl border px-4 font-bold">Start review</button><button onClick={()=>review.mutate({id:item.id,action:"correction"})} className="min-h-11 rounded-xl bg-amber-100 px-4 font-bold text-amber-900">Request correction</button><button onClick={()=>review.mutate({id:item.id,action:"approve"})} className="min-h-11 rounded-xl bg-emerald-700 px-4 font-bold text-white">Approve</button><button onClick={()=>review.mutate({id:item.id,action:"reject"})} className="min-h-11 rounded-xl bg-red-100 px-4 font-bold text-red-800">Reject</button></div>}</article>)}{query.isPending&&<p>Loading applications…</p>}</div></section>}
