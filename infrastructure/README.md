# Infrastructure

Infrastructure configuration belongs here as the deployment roadmap advances. Phase 1A keeps local orchestration in the root `docker-compose.yml` and service images beside each application.

